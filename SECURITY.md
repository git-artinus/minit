# 보안 취약점 제보

Minit은 GitHub·Slack 토큰을 OS 키체인에 저장하고, 회의 녹음과 회의록을 로컬에서 다룬다.
이와 관련한 취약점을 발견하면 **공개 이슈로 올리지 말고** 아래 경로로 알려주기 바란다.

## 제보 방법

GitHub의 [Private vulnerability reporting](https://github.com/git-artinus/minit/security/advisories/new)을 사용한다.
이 기능이 비활성 상태라면 저장소 관리자에게 직접 연락한다.

## 포함하면 좋은 것

- 영향 범위 — 어떤 데이터가 노출되거나 조작될 수 있는지
- 재현 절차
- Minit 버전(설정 하단에서 확인)과 macOS 버전

## 지원 버전

최신 릴리즈만 지원한다. 이전 버전에 대한 수정은 제공하지 않는다.

## 범위 밖

- **사용자가 직접 입력한 Slack 봇 토큰의 오남용** — 토큰은 워크스페이스 관리자가 관리한다
- **번들된 서드파티 구성요소의 취약점**(whisper.cpp·Electron 등) — 해당 프로젝트에 제보한다.
  다만 Minit이 그 구성요소를 안전하지 않게 쓰고 있다면 그것은 이 저장소의 문제다
