# AGENT.md — minit 에이전트·기여 정책

이 문서는 minit 레포에서 작업하는 **자동화 에이전트(AI 코딩 도구)와 기여자에게 공통 적용되는 레포 자체 정책**이다.
이 레포의 정책은 상위 워크스페이스·조직 공통 컨벤션보다 **우선한다**.

## 이슈 연동 (auto-close 필수)

minit은 오픈소스로 개발되며, 이슈 상태가 저장소만 봐도 최신으로 유지되어야 한다.

- **특정 이슈를 해결하는 PR은 본문에 auto-close 키워드를 반드시 포함한다(필수).**
  - 예: `Closes #12`, `Fixes #34`
  - 여러 이슈를 해결하면 각각 명시한다: `Closes #12, Closes #34`
- 해결이 아니라 **참조만** 하는 경우(부분 작업, 관련 논의)에는 `Refs #n`을 사용한다.
- 커밋 메시지 푸터에서도 동일한 규칙을 따른다. 해결 커밋이 곧 PR의 핵심이면 커밋에도 `Closes #n`을 허용한다.

> 참고: 이 규칙은 auto-close 키워드를 금지하는 외부 공통 컨벤션이 있더라도 이 레포 안에서는 이 문서가 우선한다.

## 그 외 규칙

- 커밋 메시지·PR·리뷰는 한국어, Conventional Commits(`feat:`/`fix:`/…)를 따른다.
- 머지는 merge commit 방식을 사용하고 squash 머지는 금지한다(개별 커밋 이력 보존).
- `main` 직접 커밋 금지 — 모든 변경은 `feature/*`·`fix/*`·`docs/*` 브랜치 → PR을 거친다.
