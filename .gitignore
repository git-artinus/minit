node_modules
dist
out
.DS_Store
.eslintcache
*.log*
*.local
*.tsbuildinfo

# 렌더 파이프라인 중간 산출물 — build/icon.icns(재현 가능한 결과물)만 커밋 대상
build/icon.iconset/
build/tray/

# scratch: 계획/작업 스캐폴딩, 커밋 금지
.superpowers/


# 내부 문서 전체 비공개 — 공식 문서가 생기면 !docs/<파일> 로 선택 해제
docs/
